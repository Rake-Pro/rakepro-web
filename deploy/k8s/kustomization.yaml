apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Pin the image tag here per environment, e.g.:
#   images:
#     - name: rakepro/rakepro-web
#       newTag: v0.1.0
