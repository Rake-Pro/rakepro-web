apiVersion: v1
kind: Service
metadata:
  name: rakepro-web
  labels:
    app.kubernetes.io/name: rakepro-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rakepro-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
